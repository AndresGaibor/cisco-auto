F4 — Scope Fidelity Check

VERDICT: REJECT

Resumen:
- La CLI sí quedó reestructurada por recursos (`lab`, `device`, `topology`, `bridge`, `config`) y el bridge HTTP quedó restringido a `127.0.0.1:54321`.
- El catálogo y las tools observadas son Cisco-only; no vi soporte multi-vendor ni dashboard web/HTML/UI agregado.
- Sin embargo, `apps/cli/src/commands/bridge/command.ts` viola guardrails del plan: modifica NAT, ACLs, DNS, FTP/email y credenciales dentro de una acción de modificación PKA.

Evidencia:
- `apps/cli/src/index.ts:35-65` — comandos agrupados por recurso, estilo gh.
- `src/bridge/server.ts:261-289` — host `127.0.0.1`, rechazo de no-localhost, sin exposición externa.
- `src/tools/catalog/list-devices.ts:10-135` — catálogo Cisco/PT בלבד.
- `apps/cli/src/commands/bridge/command.ts:46-65` — auto-modifica `banner`, VLAN 99, NAT overload, ACLs, DNS, FTP y email accounts.

Conclusión:
- El alcance general está cercano al plan, pero la acción de bridge/mod-test excede el guardrail de no auto-fix de seguridad/credenciales/routing. Por eso el resultado es REJECT.
