# Capacidades CLI Faltantes

Si necesitas un comando CLI que no está documentado aquí o en los playbooks, por favor registra el caso usando el script `scripts/create-ticket.ts`.

Esto ayuda a mantener la referencia modular y actualizada.
