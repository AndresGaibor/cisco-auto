## Qwen Added Memories
- @cisco-auto/file-bridge package fully documented - Bridge V2 protocol uses file-based messaging (commands/, in-flight/, results/, NDJSON logs), atomic operations (tmp+fsync+rename), lease-based single-instance, crash recovery, log rotation (32MB), sequence numbers for gap detection, checkpoint persistence. Zero test coverage for: lease management, log rotation, dead-letter queue, gap detection, checksum verification.
- @cisco-auto/pt-control package documented - High-level PT control API with PTController class, VirtualTopology (event-driven state), CliSession (IOS state machine), FileBridgeV2 (durable messaging), LogManager (NDJSON logging), ValidationEngine (rule-based). Domain-driven design with value objects (VlanId, Ipv4Address), operation planners (VLAN, trunk, SVI, static routes), IOS parsers. CLI via oclif with commands for vlan/apply, trunk/apply, ssh/setup, device/*, link/*, config/*, runtime/*. Test coverage: VLAN, SSH, IOS session, capability resolver, runtime generator, bridge V2, NDJSON consumer. Gaps: lease edge cases, log rotation under load, dead-letter handling.
- @cisco-auto/core package documented - Canonical model (LabSpec, DeviceSpec, ConnectionSpec, Protocol specs), Device Catalog (50+ devices: routers 2911/1941/4451, switches 2960/3560/3650, end devices, modules), Configuration Generators (IOSGenerator composes Base/VLAN/Routing/Security/STP/EtherChannel/Services/IPv6/PortTemplate generators), ConfigDiffer for incremental deployment, Validation (LabValidator, DeviceSpecValidator with error codes), DeployOrchestrator (SSH deployment with rollback), Tools (pt_plan_topology, pt_generate_script, pt_validate_plan, pt_deploy, pt_list_devices), Topology visualizer (ASCII, Mermaid, matrix), SSHConnector (node-ssh wrapper), ExecutionContext (DI with correlation ID, Pino logging). Test coverage: validator, generators, context. Gaps: lease edge cases, log rotation, dead-letter handling.
