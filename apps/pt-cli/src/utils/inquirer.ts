import * as inquirerPrompts from "@inquirer/prompts";

const prompts = inquirerPrompts as any;

export const select = prompts.select;
export const input = prompts.input;
export const confirm = prompts.confirm;
