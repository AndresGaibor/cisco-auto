{
  "name": "@cisco-auto/pt-runtime",
  "version": "0.1.0",
  "description": "Packet Tracer runtime generator - generates main.js and runtime.js for PT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./value-objects/validated-models": "./dist/value-objects/validated-models.js"
  },
  "bin": {
    "pt-runtime": "./bin/run.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "generate-models": "bun run src/scripts/generate-model-map.ts",
    "generate": "bun run src/scripts/generate-model-map.ts && bun run src/index.ts generate",
    "deploy": "bun run src/index.ts deploy",
    "test": "bun test"
  },
  "dependencies": {
    "@cisco-auto/core": "workspace:*"
  },
  "devDependencies": {
    "@cisco-auto/pt-control": "workspace:*",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
